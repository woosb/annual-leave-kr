# 줄바꿈은 저장소 안에서 항상 LF 로 통일한다.
# CI 가 ubuntu 러너에서 ./gradlew 를 직접 실행하므로 기여자의
# core.autocrlf 설정에 관계없이 LF 가 보장되어야 한다.
* text=auto eol=lf

# Windows 전용 스크립트만 CRLF 유지
*.bat  text eol=crlf
*.cmd  text eol=crlf

# 바이너리로 취급 (줄바꿈 변환 금지)
*.jar  binary
*.gpg  binary
*.asc  binary
